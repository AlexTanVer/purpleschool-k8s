apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.app.name }}-secret
  namespace: {{ .Values.app.namespace }}
type: Opaque
data:
  user: {{ .Values.app.config.credentials.user | b64enc | quote }}
  password: {{ .Values.app.config.credentials.password | b64enc | quote }}