apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.app.name }}-secret
  namespace: {{ .Values.app.namespace }}
type: Opaque
data:
  user: {{ .Values.credentials.user | b64enc | quote }}
  password: {{ .Values.credentials.password | b64enc | quote }}